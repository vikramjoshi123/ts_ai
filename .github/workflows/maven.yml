name: Java CI with Maven & Docker Push

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: Build with Maven
        run: mvn clean install -DskipTests

      - name: Build & push Docker image
        uses: vikramjoshi123/spring-ci-cd-test:tagname
        with:
          image: vikramjoshi123/spring-ci-cd-test
          tags: v1, latest
          dockerfile: Dockerfile
          registry: docker.io
          username: "vikramdip40@gmail.com"
          password: "Vikram@70"
